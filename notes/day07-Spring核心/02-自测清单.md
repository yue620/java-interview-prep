# Day 7 自测清单：Spring 核心

> 用法：不看讲义，逐题口述（最好计时 90 秒/题），说完再对答案。不会的标记 ❌，晚上过错题本。

1. IoC 和 DI 分别是什么？它们是什么关系？
2. 把一个类变成 Spring 管理的 Bean，有哪几种注解？它们有功能区别吗？
3. 依赖注入有哪三种方式？为什么推荐构造方法注入？
4. Bean 的默认作用域是什么？单例 Bean 线程安全吗？为什么？
5. 说出 Bean 生命周期的主要阶段（从创建到销毁）。
6. BeanPostProcessor 在生命周期的哪个位置起作用？AOP 代理是在哪里生成的？
7. 什么是循环依赖？Spring 用三级缓存怎么解决的？（能说清 A、B 创建流程）
8. 哪种循环依赖 Spring 解决不了？为什么？
9. AOP 的底层实现是什么？JDK 动态代理和 CGLIB 的区别？
10. 为什么 `this.foo()` 自调用时 AOP/事务不生效？

---

## 今日实践验收

- [ ] LifeBean 生命周期三个打印按顺序出现
- [ ] TimeAspect 成功拦截并打印方法耗时
- [ ] ISSUE-006：构造方法循环依赖报错截图/记录 + setter 注入修复后能启动 + 能口述三级缓存流程
